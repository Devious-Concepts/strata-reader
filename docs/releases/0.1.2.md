# strata-reader 0.1.2 release record

This record was produced after the release. Version 0.1.2 was published on 2026-09-24, before
the procedure in [RELEASING.md](../../RELEASING.md) existed. On 2026-09-26 that gate was run
against the tagged commit, and the package published on crates.io was tested as well.

| Field | Value |
| --- | --- |
| Commit | `d3e1f242f33d8abb896082f93a631ffc353acf6d`, tag `0.1.2` |
| Host | Linux 7.2.7-zen1-1-zen, x86_64 |
| Development compiler | rustc 1.98.1 (48a229cea 2026-09-01), cargo 1.98.1 (797e8a9bc 2026-08-05) |
| MSRV compiler | rustc 1.87.0 (17067e9ac 2025-05-09), cargo 1.87.0 (99624be96 2025-05-06) |
| Gate run | 2026-09-26, post hoc, by Claude Fable 5.1 via Claude Code |

## Results

| Stage | Result |
| --- | --- |
| Fast checks | Pass; 59 unit tests passed, 34 doctests passed and 1 ignored |
| Minimum supported Rust version 1.87.0 | Pass; the same counts |
| Package | Pass; the same counts from `cargo test --all-features` in the extracted copy |

Ignored tests: the `DynamicReadExt::fill_while` doctest in `src/read.rs` is marked `ignore`
because it has no runnable setup for `reader`. Making it run is tracked as maintenance work; it
does not affect the crate's behavior.

## Package

`cargo package --list`, 19 files:

```text
.cargo_vcs_info.json
CHANGELOG.md
CONTRIBUTING.md
Cargo.lock
Cargo.toml
Cargo.toml.orig
LICENSE
LICENSE-CLARIFICATION.md
LICENSE-MIT-0
README.md
rust-toolchain.toml
src/buffer/tests.rs
src/buffer.rs
src/constants/tests.rs
src/constants.rs
src/lib.rs
src/read.rs
src/reader/tests.rs
src/reader.rs
```

## Publication

crates.io lists 0.1.2, created 2026-09-24T11:05:57Z, with `rust-version` 1.87.0. The package
downloaded from crates.io passed `cargo test --all-features` with the same counts. docs.rs serves
the 0.1.2 build, and the Codeberg release `0.1.2` carries the changelog entry as its body.
